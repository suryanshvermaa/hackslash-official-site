apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc-hackslash
  namespace: hackslash
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce